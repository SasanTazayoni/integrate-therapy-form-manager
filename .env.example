# Backend API URL (leave as "/" if frontend and backend are served from the same origin)
VITE_API_URL=/

# Router base path (leave as "/" unless the app is deployed to a subdirectory)
VITE_BASE_PATH=/

